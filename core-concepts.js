// poncco rotnooo js er

// variable: let, const
let money = 20;
money = 12;
const name = 'Kuddus';
const person = {
    name:'Kudus',
    age: 55,
    city:'Dhaka'
}
const statement = `This person ${name} with age ${person.age}`;


// condition
if(money > 20 && age >= 45){

}else{

}

// array declaretion
const numbers = [34,54,35];
const friends = ['Motiur','jahangir'];
const product = [{name:'laptop',price: 5000},{name:'mobile',price:2445}]

// loop : for loop
for (let i = 0; i < numbers.length; i++) {
    const number = numbers[i];
    
}

// function: regular and arrow
function add(num1,num2){
    return num1+ num2;
}
const result = add(44,33);
// arrow
const fiveTimes = num => num *5;
const add2 = (num1, num2)=> num1 +num2;
const doMath = (x,y) =>{
    const sum = x+y;
    return sum*2;
}